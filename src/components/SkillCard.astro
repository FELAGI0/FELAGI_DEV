---
/**
 * One skills column: a translated category name and the technologies in it
 * (brief §7 Skills).
 *
 * `labelKey` rather than a `label` string: the category name is translated
 * ("Backend"/"Backend", "Infrastructure"/«Infrastructure»), so the component
 * resolves it against the page's locale instead of receiving a pre-translated
 * string from a caller that might pass the wrong one.
 */
import type { UIKey } from '@/i18n/ui';
import type { Lang } from '@/i18n/ui';
import { t } from '@/i18n/utils';

interface Props {
  /** Locale, used to resolve `labelKey`. */
  lang: Lang;
  /** Dictionary key for the column heading. */
  labelKey: UIKey;
  /** Technology names — proper nouns, identical in every language. */
  items: readonly string[];
}

const { lang, labelKey, items } = Astro.props;
---

<li class="skill-card">
  <h3 class="skill-card__title">{t(lang, labelKey)}</h3>
  <ul class="skill-card__items">
    {
      items.map((item) => (
        <li class="skill-card__item">{item}</li>
      ))
    }
  </ul>
</li>

<style>
  /*
   * Compact card. The parent `.skills` is a flex row, so a group with two items
   * stays narrow and a group with six is wider — density follows the data
   * instead of every tile being padded out to the same rectangle.
   *
   * `min-width: 0` matters: without it a flex item refuses to shrink below its
   * min-content width, so a card whose contents are one long row of chips would
   * hold the row wider than the column and overflow it.
   */
  .skill-card {
    flex: 0 1 auto;
    min-width: 0;
    padding: var(--space-4);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
  }

  /*
   * From 1024px the three cards share one row (see `.skills` in index.astro),
   * so they also take an equal share of any leftover space. `flex-grow: 1` adds
   * the same amount to each card's own width, which keeps their proportions —
   * Backend stays widest, Frontend narrowest — while the row ends where the
   * content column does rather than trailing off into empty space.
   */
  @media (min-width: 64rem) {
    .skill-card {
      flex-grow: 1;
    }
  }

  .skill-card__title {
    margin-bottom: var(--space-2);
    /*
     * 13px, not the 12px `--text-xs` it was: this is an uppercase label with
     * letter-spacing, and at 12px it read as noticeably smaller than every other
     * piece of chrome on the page. 13px matches the chips it labels, so the card
     * has one text size instead of two near-identical ones.
     */
    font-size: var(--text-chip);
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--accent);
  }

  /*
   * Items flow as one inline row separated by a muted "·" rather than a stacked
   * list: with two to six short names per group, stacking reads as wasted
   * vertical space. The inner list still wraps on its own when the card is
   * squeezed, so a narrow card degrades to two lines instead of overflowing.
   */
  .skill-card__items {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: var(--space-1) var(--space-2);
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .skill-card__item {
    font-family: var(--font-mono);
    /* Chip scale (brief §3) rather than body scale: these are chip-like
       tokens, and the smaller size is what lets three cards share one row. */
    font-size: var(--text-chip);
    color: var(--fg-muted);
  }

  .skill-card__item + .skill-card__item::before {
    content: '·';
    margin-inline-end: var(--space-2);
    color: var(--border-strong);
  }
</style>
